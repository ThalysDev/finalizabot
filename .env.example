# ============================================================================
# FinalizaBOT — Variáveis de Ambiente
# ============================================================================
# Copie este arquivo para .env.local e preencha os valores.
# NÃO commitar .env ou .env.local — apenas .env.example.

# ── Banco de Dados (Neon PostgreSQL) ────────────────────────────────────────
# Connection string do pooler Neon (obrigatório).
# Formato: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# Copie "Connection string" do dashboard Neon → Connection Details.
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Connection string direta (sem pooler) — usada pelo Prisma em migrações.
# Na integração Vercel-Neon, definida automaticamente como DATABASE_URL_UNPOOLED.
# Copie "Direct connection" do dashboard Neon → Connection Details.
# Necessária para `prisma migrate dev` e `prisma db push`.
DATABASE_URL_UNPOOLED="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# ── SofaScore ETL API ───────────────────────────────────────────────────────
# URL base da API HTTP do ETL (server-side only — sem NEXT_PUBLIC_).
# O ETL expõe: GET /health, /players/:id/last-matches, /players/:id/shots,
#              /matches/:id/shots
# Em dev: http://localhost:3001  |  Em prod: URL do deploy do ETL
# OPCIONAL: sem isso o dashboard mostra partidas mas sem análises detalhadas.
SOFASCORE_ETL_API_URL=http://localhost:3001

# ── Sync (GitHub Actions / manual) ─────────────────────────────────────────
# IDs de torneios SofaScore separados por vírgula. Default: principais ligas.
# SYNC_TOURNAMENTS=325,390,4407,384,480,17,8,23,35,34,7
# Data para sincronizar (YYYY-MM-DD). Default: hoje.
# SYNC_DATE=2026-02-07
# "true" para pular busca de escalações.
# SYNC_SKIP_LINEUPS=false

# ── Clerk Authentication ────────────────────────────────────────────────────
# Obtenha em https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key
CLERK_SECRET_KEY=sk_test_your_key

# URLs de redirecionamento
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ── App URL ─────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ── Google Analytics (opcional) ─────────────────────────────────────────────
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
